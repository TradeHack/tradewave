import { FormLabel } from '@material-ui/core';
import styled from 'styled-components';

export const Title = styled(FormLabel)`
  && {
    font-weight: bold;
    font-size: 25px;
    color: black;
    margin-bottom: 30px;
  }
`;
